name: Release_Tags.yml

on: [workflow_call]
jobs:
  check-bats-version:
    runs-on: self-hosted
    steps:
      - name: checkout
        uses: actions/checkout@v4

      - name: SAST scans
        run: echo SAST scans

      - name: SCA scans
        run: echo SCA scans

      - name: secret detection in source code
        run: echo secret detection in source code

      - name: artifact produce
        run: echo artifact produce